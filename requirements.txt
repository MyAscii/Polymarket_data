# poly_onchain dependencies
# Install: conda activate python3.12 && pip install -r requirements.txt

# Core dependencies
web3>=6.0.0              # Polygon RPC interaction
requests>=2.28.0         # HTTP requests (Gamma API)
pandas>=2.0.0            # Data processing
pyarrow>=12.0.0          # Parquet read/write

# Optional dependencies
eth-abi>=4.0.0           # ABI encoding/decoding (already included by web3, listed separately to pin the version)
